# ============================================================
# FILE: requirements.txt
# ============================================================
#
# NOTE: tflite-runtime is NOT listed here because it must be
# installed manually from Google's Pi wheel (see setup.sh).
# Installing it via pip on aarch64 will fail or pull the wrong build.
#
opencv-python-headless>=4.7.0
numpy>=1.21.0
PyYAML>=6.0
flask>=2.2.5
paho-mqtt>=1.6.1
Pillow>=9.0.0
python-dotenv>=0.21.1
twilio>=8.10.0
