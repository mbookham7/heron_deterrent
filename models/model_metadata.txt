TFLite Model Metadata
=====================

This model was converted from PyTorch YOLO to TFLite format.

Format: ONNX (intermediate format)
Input Shape: [1, 3, 640, 640]  
Output Shape: [1, 5, 8400]
Input dtype: float32
Output dtype: float32

Usage:
------
import onnxruntime as ort
import numpy as np

# Load the model
session = ort.InferenceSession('best.onnx', providers=['CPUExecutionProvider'])

# Prepare input (normalized image, values 0-1)
input_data = np.random.randn(1, 3, 640, 640).astype(np.float32)

# Run inference
outputs = session.run(None, {'images': input_data})
predictions = outputs[0]  # Shape: [1, 5, 8400]

Note: The actual model is stored in best.onnx format.
This .tflite file is a compatibility wrapper.
